<%=render'users/user_info', book: @book, user: @user  %>


<div class=col-md-9>
    <h2>Books</h2>


    <table class="table table-hover ">
        <thead>
            <tr class=tr-top><tr>
            <th>title</th>
            <th>opinion</th>
            <tr>
        </thead>
        <tbody>
        <% @books.each do |book| %>
            <tr>
            <tb><%= attachment_image_tag @user, :profile_image_id, :fill,40, 40, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %></td>
            <td><%= book.title %></tb>
            <td><%= book.body %></td>
            </tr>
        </tbody>
        <% end %>
    </table>
</div>