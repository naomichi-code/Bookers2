<%=render'users/user_info', book: @book, user: @book_show.user  %>

<div class=col-md-9>
    <h2>Book detail</h2>


    <table class="table table-hover ">
        <thead>
            <tr class=tr-top><tr>
            <th>title</th>
            <th>opinion</th>
            <tr>
        </thead>
        <tbody>
            <tr>
            <td><%= attachment_image_tag @book_show.user, :profile_image, :fill,40, 40, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %></td>
            <td><%= @book_show.title %></tb>
            <td><%= @book_show.body %></td>
            </tr>
        </tbody>

    </table>
</div>